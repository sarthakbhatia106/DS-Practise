import React from 'react'

function About({isAuth}) {
    console.log(isAuth);
    return (
        <div>
            <h1>This is the about component</h1>
        </div>
    )
}

export default About
